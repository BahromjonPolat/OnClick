import 'package:flutter/material.dart';

class CustomNavigatorInit extends StatelessWidget {
  final Widget builder;
  const CustomNavigatorInit({Key? key, required this.builder}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    
   return builder;
  }

}